using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaserBullet : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        EnemyStart();
        SetValue(8, 10f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
