using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VeteranBullet : EntityState
{

    // Start is called before the first frame update
    void Start()
    {
        EnemyStart(5, 1.5f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Death()
    {
        
    }
}
